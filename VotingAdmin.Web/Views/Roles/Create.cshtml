@using VotingAdmin.Web.Dtos.Roles;

@model RoleDto
@*<form id="form" asp-action="Create" onsubmit="if(validate())return PopUPAjaxPost2(this);" autocomplete="off">*@
<form id="formRoles" method="post" autocomplete="off">
    <ul>
        @if (ViewBag.Error is not null)
        {
            @foreach (var error in ViewBag.Error)
            {
                <li class="text-danger">@error</li>
            }
        }
    </ul>
    <div class="row">
        <div class="col-sm-12 mb-3">
            <label for="exampleInputEmail1">Role Name</label>
            <input asp-for="roleName" type="text" class="form-control" id="exampleInputEmail1" placeholder="Enter Role Name..">
            <span asp-validation-for="roleName" class="text-danger"></span>

        </div>
        <div class="col-sm-12 mb-3">
            <label for="exampleInputEmail1">Access</label>
            <input asp-for="description" type="text" class="form-control" id="Discription">
        </div>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-sm btn-light" data-dismiss="modal">Cancel</button>
        <button type="submit" class="btn btn-sm btn-primary">Save changes</button>
    </div>
</form>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
<script>

      FormHandler.init({
        method: 'POST',
        actionUrl: '@Url.Action("Create", "Roles")',
        containerSelector: '#add-new .modal-body',
          selector: '#formRoles'
      }, function () {
        $("#add-new .modal-body").html("");
        $("#add-new .modal-title").html("");
          $("#add-new").modal('hide');
          //new PNotify({
          //    // title: 'Success ',
          //    text: 'Rates Created Successfully',
          //    type: 'success',
          //    animation: {
          //        effect_in: 'fade',
          //        effect_out: 'slide'
          //    }

          //});
        Pagination.getData();
    });
</script>